import { Place, SchoolLevel } from '../../../../core/auth/_models';
import { Year } from '../../../../core/year';

export class SmasConText {
  level: SchoolLevel | null;
  year: Year | null;
  place: Place | null;
}
